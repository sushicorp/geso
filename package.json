{
  "name": "geso",
  "version": "1.0.0",
  "description": "CSS framework",
  "main": "index.js",
  "dependencies": {
    "animate.css": "^3.2.5",
    "nib": "^1.1.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "stylus -u nib -w main.styl -o dist/bundle.css",
    "build": "stylus -u nib main.styl -o dist/bundle.css",
    "preview": "http-server & wtch"
  },
  "author": "",
  "license": "ISC"
}
